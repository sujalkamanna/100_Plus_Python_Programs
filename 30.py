# Program 30
# Write a Python Program to calculate the natural logarithm of any number.

import math

no = float(input("Enter a no to convert it into natural logarithm:"))

log = math.log(no)
print(f"natural log of {no} is {log}")